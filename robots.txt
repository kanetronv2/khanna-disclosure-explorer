User-agent: *
Allow: /

# The lazy per-year chunks are machine plumbing with nothing to index, and they are the
# bulk of what a crawler would otherwise pull. summaries.js stays crawlable because every
# page runs it on load, so blocking it would break how Google renders the overview.
Allow: /site-data/summaries.js
Disallow: /site-data/

# Page scans and source PDFs stay crawlable on purpose: they are the evidence behind every
# figure. The OCR side-channels (raw text, Tesseract dumps, crops) are not deployed at all,
# so they need no rule here.

Sitemap: https://www.rokhanna.money/sitemap.xml
